{{serverEndpoints}}

sv_hostname "{{serverName}} built with {{recipeName}} by {{recipeAuthor}}!"
sv_maxclients {{maxClients}}
sv_licenseKey "{{svLicense}}"
load_server_icon oxpog.png
sv_scriptHookAllowed 0

set sv_enforceGameBuild 2545
set mysql_connection_string "{{dbConnectionString}}"
set steam_webApiKey "none"

sets tags "overextended, ox_core"
sets locale "en-US" 
sets sv_projectName "[{{recipeName}}] {{serverName}}"
sets sv_projectDesc "{{recipeDescription}}"

## Resouces
add_ace resource.ox_core command.add_principal allow
add_ace resource.ox_core command.remove_principal allow

ensure chat
ensure spawnmanager
ensure sessionmanager
ensure baseevents

ensure oxmysql
ensure pe-lualib
ensure fivem-appearance
ensure ox_core
ensure pma-voice
ensure ox_inventory
ensure pe-ui
ensure npwd
ensure pe-basicloading

## Admins
add_ace group.admin command allow # allow all commands
add_ace group.admin command.quit deny # but don't allow quit
{{addPrincipalsMaster}}
